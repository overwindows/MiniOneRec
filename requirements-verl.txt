# VERL framework for RL training
# Pin to specific commit compatible with torch 2.6.0
git+https://github.com/volcengine/verl.git@v0.3.0.post1

# vLLM rollout backend - pin to version compatible with torch 2.6.0
vllm==0.6.6.post1

# ZeroMQ for vLLM async communication
pyzmq

# Optional: SGLang rollout backend (alternative to vLLM)
# sglang

# Additional dependencies for VERL that may not be auto-installed
ray[default]
tensordict
codetiming
pylatexenc

# Numpy version constraint (verl requires numpy<2.0.0)
numpy<2.0.0
